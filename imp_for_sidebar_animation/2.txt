.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  z-index: 85;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease, visibility 0.3s ease;
}

.overlay.active {
  opacity: 1;
  visibility: visible;
}

/* Sidebar styles */
.sidebar {
  position: fixed;
  top: 0;
  right: 0;
  height: 100vh;
  z-index: 90;
  width: 16.16%;
  min-width: 250px;
  font-size: 2rem;
  background-color: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(18px);
  box-shadow: -10px 0 10px rgba(0, 0, 0, 0.1);
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: flex-start;
  gap: 5%;
  padding-top: 60px;
  list-style: none;

  /* Animation properties - starts hidden */
  transform: translateX(100%);
  transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.sidebar.show {
  transform: translateX(0);
}

.sidebar li {
  width: 100%;
  opacity: 0;
  transform: translateX(20px);
  transition: all 0.1s ease, opacity 0.2s ease, transform 0.2s ease;
}

.sidebar.show li {
  opacity: 1;
  transform: translateX(0);
}

/* Staggered animation for list items */
.sidebar.show li:nth-child(1) {
  transition-delay: 0.1s;
}
.sidebar.show li:nth-child(2) {
  transition-delay: 0.15s;
}
.sidebar.show li:nth-child(3) {
  transition-delay: 0.2s;
}
.sidebar.show li:nth-child(4) {
  transition-delay: 0.25s;
}
.sidebar.show li:nth-child(5) {
  transition-delay: 0.3s;
}
.sidebar.show li:nth-child(6) {
  transition-delay: 0.35s;
}
.sidebar.show li:nth-child(7) {
  transition-delay: 0.4s;
}

.sidebar li a {
  color: rgb(255, 255, 255);
  text-decoration: none;
  display: block;
  width: 100%;
  padding: 0.5rem 1rem;
  transition: color 0.2s ease;
  border-radius: 0.5rem;
}

.sidebar li:hover {
  transition: all 0.1s ease;
  background-color: rgba(255, 255, 255, 0.967);
  border-radius: 0.5rem;
}

.sidebar li:hover a {
  transition: all 0.2s ease;
  color: rgb(0, 0, 0);
  transform: scale(1.1);
}

.close {
  color: rgb(255, 255, 255);
  cursor: pointer;
}

.close svg {
  color: white;
  transition: transform 0.2s ease;
}

.close:hover svg {
  color: black;
  transform: rotate(90deg) scale(1.1);
}